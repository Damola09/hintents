version: 1

run:
  timeout: 5m
  modules-download-mode: readonly
  go: "1.24"

linters:
  disable-all: true
  enable:
    - ineffassign
    - govet

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
